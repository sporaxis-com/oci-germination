name: core-pg17-nats-micro-release

on:
  pull_request:
  push:
    branches:
      - main
    tags:
      - core-pg17-nats-micro-v*
  schedule:
    - cron: '47 4 * * *'
  workflow_dispatch:

permissions:
  contents: write
  packages: write

concurrency:
  group: core-pg17-nats-micro-${{ github.ref }}
  cancel-in-progress: false

jobs:
  verify:
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - uses: docker/setup-buildx-action@v3

      - name: Run tests
        run: go test ./...

      - name: Regenerate bundle outputs
        run: bash scripts/generate.sh

      - name: Verify generated files are committed
        run: git diff --exit-code -- bundles/

      - name: Build native NATS micro image
        run: bash scripts/build-core-pg17-nats-micro.sh

      - name: Smoke test native NATS micro image
        run: bash scripts/smoke-core-pg17-nats-micro.sh

  publish:
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/core-pg17-nats-micro-v')
    needs: verify
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Ensure tagged commit is on main history
        run: |
          set -euo pipefail
          git fetch --no-tags origin main
          if ! git merge-base --is-ancestor "$GITHUB_SHA" "origin/main"; then
            echo "refusing to publish tag outside origin/main history: $GITHUB_SHA" >&2
            exit 1
          fi

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Regenerate bundle outputs
        run: bash scripts/generate.sh

      - uses: docker/setup-qemu-action@v3

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}

      - name: Resolve release coordinates
        id: meta
        run: |
          set -euo pipefail
          echo "image=ghcr.io/${GITHUB_REPOSITORY_OWNER,,}/ociger-core-pg17-nats-micro" >> "$GITHUB_OUTPUT"
          echo "tag=${GITHUB_REF_NAME#core-pg17-nats-micro-}" >> "$GITHUB_OUTPUT"

      - name: Publish multi-arch image
        run: |
          set -euo pipefail
          docker buildx build \
            --platform linux/amd64,linux/arm64 \
            --file bundles/core-pg17-nats-micro/Dockerfile \
            --label "org.opencontainers.image.source=https://github.com/${GITHUB_REPOSITORY}" \
            --label "org.opencontainers.image.version=${{ steps.meta.outputs.tag }}" \
            --label "org.opencontainers.image.description=Minimal PostgreSQL 17 micro runtime with colocated NATS core and websocket listeners" \
            --tag "${{ steps.meta.outputs.image }}:${{ steps.meta.outputs.tag }}" \
            --tag "${{ steps.meta.outputs.image }}:latest" \
            --provenance=false \
            --push \
            .

      - name: Inspect published image
        run: docker buildx imagetools inspect "${{ steps.meta.outputs.image }}:${{ steps.meta.outputs.tag }}"

      - name: Attempt to make GHCR package public
        continue-on-error: true
        env:
          GH_TOKEN: ${{ github.token }}
        run: bash scripts/ensure-ghcr-public.sh ociger-core-pg17-nats-micro

      - uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          body: |
            Published `${{ steps.meta.outputs.image }}:${{ steps.meta.outputs.tag }}` and `${{ steps.meta.outputs.image }}:latest`.

            Local launch:

            ```bash
            docker run --rm -d \
              --name ociger-core-pg17-nats-micro \
              -e PGDATA=/var/lib/postgresql/data \
              -v "$PWD/ociger-core-pg17-nats-micro-data:/var/lib/postgresql/data" \
              -p 15437:5432 \
              -p 24222:4222 \
              -p 29222:9222 \
              ${{ steps.meta.outputs.image }}:${{ steps.meta.outputs.tag }}
            ```
