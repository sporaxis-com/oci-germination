.DS_Store
Thumbs.db

.idea/
.vscode/
*.swp
*.swo
*~

.env
.env.*
!.env.example
.direnv/

__pycache__/
.pytest_cache/
.venv/
venv/

node_modules/

build/
dist/
out/
tmp/
temp/
coverage/

*.log
*.pid

postgres-data/
.pgdata/
data/

*.tar
*.tar.gz
*.tgz
*.img
*.oci

.artifacts/
.worktrees/

coverage.out
*.coverprofile

# Claude Code session state
.claude/

# WIP scratch (NOTIFIES, drafts, transient docs — local exchange only)
_WIP/

# hello-kernel example: per-run scratch (bundle's cklib staged for node:22)
examples/hello-kernel/.run-*

# Playwright MCP snapshot scratch (local browser-tool artifact)
.playwright-mcp/

# stray go-build artifacts (built binaries land in repo root)
/ociger-ck-identity
/ociger-pg-launcher
/ociger-supervisor
/ociger-static-server

# CK-org wave documents + specs — local exchange only, never public.
# CKP.*.VISION-REQUIREMENTS-STATEMENT.md, SPEC.*, bench notes.
CK*.md
SPEC*.md
# Exception: component engineering specs are tracked (pgRDF's form, CK-org 16.5).
!specs/SPEC.oci-germination.*.md
